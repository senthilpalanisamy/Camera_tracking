cmake_minimum_required(VERSION 3.0)
project(example)
find_package(OpenCV REQUIRED)  
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(object_tracking objectTracking.cpp)
target_link_libraries(object_tracking ${OpenCV_LIBS})
